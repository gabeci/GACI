import { MobileShell } from "@/components/mobile-shell";
import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function ChatbotPage() {
  return (
    <MobileShell>
      <ScreenPlaceholder
        title="Chatbot"
        description="DM-like placeholder where Emotion Capture, Meaning Convergence, and one Alignment Action can be guided."
      />
    </MobileShell>
  );
}
